# Assets

This folder contains two kinds of files:

- `templates/`: placeholder-based starter files to generate new config
- `examples/`: concrete examples showing the expected final structure

Use the templates when creating files.
Use the examples to understand the intended shape and conventions.
